{
  "name": "@loomai/ai-providers",
  "version": "0.1.0",
  "type": "module",
  "description": "Vercel AI SDK-style provider registry for local and remote LLM providers",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@loomai/types": "file:../types",
    "@loomai/config": "file:../config"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "typescript": "^5.4.0"
  }
}
